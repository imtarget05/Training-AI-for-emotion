# Development / test dependencies (pip install -r requirements-dev.txt)
-r requirements.txt
pytest>=8.0
# Optional experiment tracking (Phase: MLOps). evaluate.py degrades gracefully
# if mlflow is absent, so this is dev-only — NOT needed at serving time.
mlflow>=2.14